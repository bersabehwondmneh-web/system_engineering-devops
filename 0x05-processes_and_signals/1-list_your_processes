#!/usr/bin/env bash
# This script lists all currently running processes on the system with a user-friendly view
The 'ps' command with -e shows all processes
# -f gives full-format listing including user, PID, parent PID, etc.
# --forest visually shows process hierarchy
# --cols=200 ensures wide output is not truncated (optional for readability)
ps -ef --forest --cols=200
